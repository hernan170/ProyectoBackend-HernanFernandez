<div class="container my-5">
    <h1 class="mb-4 text-center">Catálogo de Productos (Renderizado Estático)</h1>
    <p class="text-center text-muted">Esta lista se carga una sola vez al iniciar la página. Para ver la funcionalidad en tiempo real, visita la sección "Tiempo Real" en el menú.</p>
    {{#if products}}
    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle shadow-sm">
            <thead class="table-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Título</th>
                    <th scope="col">Descripción</th>
                    <th scope="col">Precio</th>
                    <th scope="col">Stock</th>
                    <th scope="col">Código</th>
                </tr>
            </thead>
            <tbody>
                {{#each products}}
                <tr>
                    <th scope="row">{{this.id}}</th>
                    <td>{{this.title}}</td>
                    <td>{{this.description}}</td>
                    <td>${{this.price}}</td>
                    <td>{{this.stock}}</td>
                    <td>{{this.code}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    {{else}}
    <div class="alert alert-warning text-center" role="alert">
        No se encontraron productos en el sistema.
    </div>
    {{/if}}

    <div class="mt-5 text-center">
        <a href="/realtimeproducts" class="btn btn-primary btn-lg shadow">Ir a la Gestión en Tiempo Real (Socket.io)</a>
    </div>
</div>